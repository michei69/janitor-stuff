export default async function patchMbxM() {
    //@ts-ignore shut up
    // for (let sc of document.querySelectorAll("script")) 
    //     if (sc.innerHTML.includes("mbxM.push(")) 
    //         wnd.Janitor.InitState = JSON.parse(
    //             sc.match(/parse\("(.*)"\)\);/)[1]
    //         )

}